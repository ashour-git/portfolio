# Groq API key for the Engineering Copilot route (server-side only).
GROQ_API_KEY=your_key_here

# Groq model to use for the Copilot. Must be served to the configured key's
# project. The server verifies it against the Groq Models API before streaming.
GROQ_MODEL=llama-3.3-70b-versatile